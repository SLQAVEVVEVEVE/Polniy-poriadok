# Update .env.example with the new values
@'
# Public Variables
NEXT_PUBLIC_SITE_URL=https://stroykomplekt-msk.ru
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
NEXT_PUBLIC_METRIKA_ID=XXXXXXXXXX

# Email Configuration
EMAIL_FROM=info@stroykomplekt-msk.ru  # Sender email (must be verified in Brevo)
EMAIL_FROM_NAME=ООО «Стройкомплект»   # Sender name
EMAIL_TO=info@stroykomplekt-msk.ru    # Recipient email for contact forms
RATE_LIMIT_DISABLED=false
RESEND_API_KEY=  # Add your Resend API key here

# Optional: Development overrides
# NODE_ENV=development
# NEXT_PUBLIC_VERCEL_URL=  # Auto-set on Vercel, can override locally if needed
'@ | Out-File -Encoding utf8 .env.example